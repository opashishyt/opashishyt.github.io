# Admin Panel Security Configuration

# Require authentication for admin access
AuthType Basic
AuthName "Admin Area - Microbiology Research Lab"
AuthUserFile /path/to/.htpasswd  # Update with actual path
Require valid-user

# Additional security headers for admin
<IfModule mod_headers.c>
    # Prevent caching of admin pages
    Header set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
    Header set Pragma "no-cache"
    Header set Expires "0"
    
    # Additional XSS protection
    Header set X-XSS-Protection "1; mode=block; report=/security/xss-report"
</IfModule>

# Block access to sensitive files
<FilesMatch "\.(js|json|log|htaccess)$">
    Require all denied
</FilesMatch>

# Custom error pages for admin
ErrorDocument 401 /error/401.html
ErrorDocument 403 /error/403.html
ErrorDocument 404 /error/404.html
ErrorDocument 500 /error/500.html

# Prevent directory listing
Options -Indexes

# Disable server signature
ServerSignature Off

# Rate limiting (requires mod_ratelimit)
# <IfModule mod_ratelimit.c>
#     <Location "/admin/">
#         SetOutputFilter RATE_LIMIT
#         SetEnv rate-limit 10
#     </Location>
# </IfModule>
